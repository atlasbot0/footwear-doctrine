# Footwear Doctrine — canonical repo

Working copy lives inside the Rollie Obsidian vault:
`~/ObsidianVaults/Rollie/5 Wiki/Footwear Doctrine/`

Any commit here IS a canonical publish. Consumed by:
- `LebonVibes/Storytelling` (Render) via git submodule at `doctrine/`
